.header_nav_dropdown {
	display: flex;
	max-width: 237px;
	width: 100%;
	flex-direction: column;
	align-items: flex-start;
	gap: var(--space-xxs);
	border-radius: var(--border-radius-large);
	background-color: var(--color-grey-light-25);
	box-shadow: 0px 2px 12px 0px var(--colors-grey-light-100);
}

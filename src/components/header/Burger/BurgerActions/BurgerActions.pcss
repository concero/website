.burger_actions {
	display: flex;
	flex-direction: row;
	width: 100%;
	align-items: flex-start;
	gap: var(--space-xs);
	margin-top: var(--space-m);
}

.burger_action_button {
	border-radius: var(--border-radius-xxxlarge) !important;
}

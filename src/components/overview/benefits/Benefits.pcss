.overview_benefits {
	display: flex;
	flex-direction: column;
	padding: var(--space-xl);
	align-items: center;
	gap: var(--space-xl);
	width: 100%;
}

.overview_benefits_cards {
	display: flex;
	align-items: flex-start;
	gap: var(--space-xl);
	width: 100%;
}

@media (max-width: 640px) {
	.overview_benefits_cards {
		flex-wrap: wrap;
		row-gap: 32px;
		column-gap: 16px;
	}
}

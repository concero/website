.permissionless {
	display: flex;
	flex-direction: column;
	align-items: flex-start;
	width: 100%;
	gap: var(--space-l);
}

.permissionless_description_container {
	display: flex;
	flex-direction: column;
	align-items: flex-start;
	width: 100%;
	gap: var(--space-xs);
}

.permissionless_title_container {
	display: flex;
	position: relative;
	gap: var(--space-xs);
}

.permissionless_title {
	font-family: var(--font-body-medium-family);
	font-size: 2rem;
	font-weight: 400;
	line-height: 2.25rem;
	color: var(--color-gray-light-700);
}

.permissionless_index {
	font-family: var(--font-body-medium-family);
	font-size: 1rem;
	font-weight: 300;
	line-height: 1.25rem;
	color: var(--color-gray-light-600);
	margin-top: -4px;
	margin-bottom: auto;
	align-self: flex-start;
	padding-top: 0;
}

.permissionless_subtitle {
	font-family: var(--font-body-medium-family);
	font-size: 1.125rem;
	font-weight: 300;
	line-height: 1.375rem;
	color: var(--color-gray-light-600);
}

.permissionless_content_container {
	display: flex;
	flex-direction: row;
	width: 75%;
	gap: var(--space-xl);
	flex-wrap: nowrap;
}

.permissionless_illustration_container {
	display: flex;
	justify-content: center;
	align-items: center;
	width: 100%;
	position: relative;
	border-radius: var(--border-radius-large);
	background: var(--color-gray-light-50);
	aspect-ratio: 1021/395;
	padding: var(--space-l);
	box-sizing: border-box;
}

.permissionless_illustration {
	width: 100%;
	height: 100%;
	object-fit: contain;
	display: block;
}

.permissionless_divider {
	height: 1px;
	width: 100%;
	background-color: var(--color-gray-light-100);
}

@media (min-width: 1025px) and (max-width: 1920px) {
	.permissionless_title {
		font-size: 1.5rem;
		line-height: 1.25rem;
		letter-spacing: -0.48px;
	}

	.permissionless_subtitle {
		font-size: 1rem;
		line-height: 1.25rem;
	}

	.permissionless_illustration_container {
		aspect-ratio: 661/256;
	}
}

@media (min-width: 641px) and (max-width: 1024px) {
	.permissionless_title {
		font-size: 1.5rem;
		line-height: 1.25rem;
		letter-spacing: -0.48px;
	}

	.permissionless_subtitle {
		font-size: 1rem;
		line-height: 1.25rem;
	}

	.permissionless_content_container {
		width: 100%;
	}

	.permissionless_illustration_container {
		aspect-ratio: 661/256;
	}
}

@media (max-width: 640px) {
	.permissionless_title {
		font-size: 1.5rem;
		line-height: 1.25rem;
		letter-spacing: -0.48px;
	}

	.permissionless_subtitle {
		font-size: 1rem;
		line-height: 1.25rem;
	}

	.permissionless_content_container {
		width: 100%;
		gap: var(--space-l);
		justify-content: space-between;
	}

	.permissionless_illustration_container {
		aspect-ratio: 1.4;
	}
}

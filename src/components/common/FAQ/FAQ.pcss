.faq_container {
	display: flex;
	flex-direction: column;
	align-items: flex-start;
	max-width: 748px;
	margin: 0 auto;
	width: 100%;
	gap: var(--space-m);
}

.faq_divider {
	height: 1px;
	width: 100%;
	background-color: var(--color-gray-100);
}

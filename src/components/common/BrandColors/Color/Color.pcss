.color_container {
	display: flex;
	flex-direction: column;
	align-items: flex-start;
	gap: var(--space-xs);
	width: 100%;
	flex: 1 0 0;
}

.color_display {
	min-height: 128px;
	width: 100%;
	border-radius: var(--border-radius-small);
}
